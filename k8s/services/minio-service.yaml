apiVersion: v1
kind: Service
metadata:
  name: minio
  namespace: marketing
spec:
  selector:
    app: minio
  ports:
    - name: api
      port: 9000
      targetPort: 9000
      nodePort: 30005
    - name: console
      port: 9001
      targetPort: 9001
      nodePort: 30006
  type: NodePort
